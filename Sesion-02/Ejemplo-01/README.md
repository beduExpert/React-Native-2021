# Instrucciones

Utilizar la app que existe ya en `StyleSheetPreview.js` dentro de la app que creamos en un principio agregando esta siguiente linea de código dentro del archivo `App.js`

```JS
import App from './StyleSheetPreview.js';
```